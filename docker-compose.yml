# Base docker-compose — definisi minimal yang digunakan oleh semua environments.
# Jangan jalankan file ini langsung; gunakan salah satu:
#   Dev  : docker-compose up          (auto-merge dengan docker-compose.override.yml)
#   Prod : docker-compose -f docker-compose.yml -f docker-compose.prod.yml up
services:
  php:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: checkin_php
    working_dir: /var/www/html
    networks:
      - checkin_network
    depends_on:
      - mysql
      - redis

  nginx:
    image: nginx:alpine
    container_name: checkin_nginx
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - checkin_network
    depends_on:
      - php

  mysql:
    image: mysql:8.0
    container_name: checkin_mysql
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - checkin_network
    command: --default-authentication-plugin=mysql_native_password

  redis:
    image: redis:7-alpine
    container_name: checkin_redis
    volumes:
      - redis_data:/data
    networks:
      - checkin_network
    command: redis-server --appendonly yes

networks:
  checkin_network:
    driver: bridge

volumes:
  mysql_data:
    driver: local
  redis_data:
    driver: local
